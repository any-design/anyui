// Type declarations for generated preview modules.
// The `.anyui-previews/` directory is generated at build time by
// vite/preview-plugin.ts from the component docs' ```vue code blocks.
declare module '*/.anyui-previews/registry' {
  const registry: Record<string, () => Promise<any>>;
  export default registry;
}