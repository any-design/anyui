---
import * as tokensDoc from '../../../../../docs/theme/tokens.md';

import DocsLayout from '../../layouts/DocsLayout.astro';

const headings = (tokensDoc as any).getHeadings?.() || [];
---

<DocsLayout title="Design Tokens" active="tokens" headings={headings}>
  <tokensDoc.Content />
</DocsLayout>
