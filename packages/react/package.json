{
  "name": "@any-design/anyui-react",
  "version": "0.5.0",
  "private": true,
  "description": "React package for AnyUI",
  "type": "module",
  "main": "../../dist/react/index.mjs",
  "module": "../../dist/react/index.mjs",
  "types": "../../dist/react/index.d.ts",
  "scripts": {
    "build": "tsup --config tsup.config.ts && tsc -p tsconfig.build.json",
    "clean": "rimraf ../../dist/react",
    "lint": "tsc -p tsconfig.json --noEmit",
    "lint-fix": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@iconify/react": "^6.0.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
